# Quick Start
# DevTools Project
[![My Java CI](https://github.com/reallyloud/devtools/actions/workflows/ci.yml/badge.svg)](https://github.com/reallyloud/devtools/actions/workflows/ci.yml)
### Чтобы запустить задачи необходимо дважды нажать run, build или test через Gradle tool Window или Run Anything (ctrl + ctrl)
# Packages
### Пакет ru.mentee.power нужен для порядка в проекте и правильной видимости классов
# Variables Table 
| Тип     | Имя переменной/метода |
| ------- | --------------------- |
| String  | menteeName            |
| int     | sprintNumber          |
| int     | plannedHoursPerWeek   |
| boolean | readyForSprint()    |
### Ссылка на урок
https://mentee-power.xl.ru/learn/MCIneBj4KkyH-GIRCspFvA/tasks

## Сценарий ручной проверки DVT-7

### Запуск приложения
1. Откройте Gradle Tool Window (View → Tool Windows → Gradle)
2. Выполните: devtools → Tasks → application → run
3. Ожидаемый вывод в Run Tool Window:
   Суммарно: пройдено 25 из 36 уроков, осталось 11 уроков

### Запуск тестов
1. Откройте Gradle Tool Window
2. Выполните: devtools → Tasks → verification → test
3. Ожидаемый вывод: BUILD SUCCESSFUL, все тесты зелёные

### Отладка через Debug
1. Установите breakpoint на строке цикла while в ProgressTracker.calculateProgress
2. Запустите Debug: кликните правой кнопкой на main → Debug 'ProgressTracker.main()'
3. Используйте Step Over (F8) для прохождения итераций
4. Проверьте Variables: counter, remainingHours должны изменяться корректно
5. Используйте Evaluate Expression (Alt+F8): вычислите remainingLessons * 2
6. Ожидаемый результат Evaluate: 14 (для completedLessons=5, totalLessons=12)

### Что делать при ошибках
- Если вывод некорректен: проверьте логику цикла через Debug
- Если тесты красные: откройте вывод теста, найдите AssertionError, скорректируйте метод
- Если Debug не останавливается: убедитесь, что breakpoint установлен (красный кружок)

## Кодстайл-гайд проекта devtools

Проект следует правилам Google Java Style Guide с адаптацией.
Автоматическая проверка: ./gradlew checkstyleMain

### 1. Именование методов: camelCase

До:    public void add_student(Student s) { }
После: public void addStudent(Student student) { }

Почему: Java Convention требует camelCase для методов.
Источник: https://google.github.io/styleguide/javaguide.html#s5.3-camel-case

### 2. Пробелы после if/for/while

До:    if(condition) {
После: if (condition) {

Почему: улучшает читаемость, отделяет ключевое слово от выражения.
Источник: Oracle Code Conventions — Whitespace

### 3. Длина строки: максимум 120 символов

До:    public List getStudentsFromSpecificCityWithVeryLongName...
После: public List getStudentsByCity(String city) {

Почему: длинные строки затрудняют чтение в редакторе и при code review.
Источник: https://google.github.io/styleguide/javaguide.html#s4.4-column-limit

### 4. Порядок импортов

До:    import java.util.List; import java.util.ArrayList; import java.io.File;
После: import java.io.File; import java.util.ArrayList; import java.util.List;

Почему: алфавитный порядок упрощает поиск импортов.
Источник: IntelliJ IDEA → Code → Optimize Imports

### 5. Фигурные скобки для if

До:    if (condition) doSomething();
После: if (condition) { doSomething(); }

Почему: скобки обязательны даже для однострочных блоков.
Источник: https://google.github.io/styleguide/javaguide.html#s4.1.1-braces-always-used

## Code Review Checklist

Используйте этот чеклист для само-ревью перед запросом ревью у ментора:

### Функциональность
- [ ] Код решает поставленную задачу полностью
- [ ] Обработаны граничные случаи (null, пустые данные, экстремальные значения)
- [ ] Обработка ошибок реализована корректно

### Тесты
- [ ] Добавлены тесты для нового функционала (или обновлены существующие)
- [ ] Все тесты проходят локально: `./gradlew test`
- [ ] Покрыты позитивные и негативные сценарии
- [ ] JaCoCo coverage ≥ 80% для нового кода

### Читаемость и стиль
- [ ] Имена переменных, методов и классов отражают назначение
- [ ] Нет дублирования кода (DRY principle)
- [ ] Checkstyle проходит без ошибок: `./gradlew checkstyleMain`
- [ ] Нет закомментированного кода или отладочного вывода (`System.out.println`)

### Документация
- [ ] README обновлён (если добавлена новая функциональность)
- [ ] Публичные методы имеют JavaDoc (если применимо)
- [ ] Примеры использования актуальны
- [ ] Runbook обновлён (если изменились команды запуска/проверки)

### Производительность и безопасность
- [ ] Нет очевидных проблем производительности
- [ ] Нет хардкода паролей, токенов или конфиденциальных данных

## Примеры Code Review комментариев

### Хорошие комментарии (конструктивные)

**Пример 1:**

**Проблема:** Метод `calculateDiscount` (строка 45) имеет 3 вложенных if-else и 40 строк.
**Почему это важно:** Сложная логика плохо тестируется и тяжело поддерживается.
**Предложение:** Вынести каждое условие в отдельный метод (например, `isEligibleForBonusDiscount()`)
и использовать паттерн Strategy для разных типов скидок.



**Пример 2:**

**Проблема:** Тест `testProcessOrder` (строка 78) проверяет только успешный сценарий.
**Почему это важно:** Не проверена обработка ошибок при недостаточном балансе.
**Предложение:** Добавить тест `testProcessOrder_InsufficientBalance_ThrowsException()`
с использованием `assertThatThrownBy()`.



### Плохие комментарии (неконструктивные)

**Пример 1:**

Этот код ужасен, полностью переписать.


**Почему плохо:** Нет конкретики (что именно плохо), нет предложения (как исправить),
токсичный тон (демотивирует автора).

**Пример 2:**

Здесь лучше использовать Stream API.


**Почему плохо:** Нет объяснения почему лучше, нет примера как переписать,
неясно какую проблему это решает.

## Информационный поиск — результаты DVT-11

### Запросы и источники

| № | Запрос                        | Операторы                                                         | Официальный источник                                                                         | Альтернатива                                                                                       | Статус | Дата проверки |
|---|-------------------------------|-------------------------------------------------------------------|----------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------|--------|---------------|
| 1 | Lombok Gradle Short           | site:search.maven.org "lombok" OR site:mvnrepository.com "lombok" | https://search.maven.org/artifact/org.projectlombok/lombok/1.18.30/jar                       | https://mvnrepository.com/artifact/org.projectlombok/lombok/1.18.30                                | 200 OK | 14.12.2025    |
| 2 | Java Stream API документация  | site:docs.oracle.com "stream api" intitle:api java 25             | https://docs.oracle.com/en/java/javase/24/docs/api/java.base/java/util/stream/Gatherers.html | https://www.baeldung.com/java-8-streams-introduction                                               | 200 OK | 14.12.2025    |
| 3 | горячие клавиши IntelliJ IDEA | site:www.jetbrains.com/help "keymap" "intellij idea"              | https://www.jetbrains.com/help/idea/mastering-keyboard-shortcuts.html                        | https://resources.jetbrains.com/storage/products/intellij-idea/docs/IntelliJIDEA_ReferenceCard.pdf | 200 OK | 14.12.2025    |
|   |                               |                                                                   |                                                                                              |                                                                                                    |        |               |
|   |                               |                                                                   |                                                                                              |                                                                                                    |        |               |
|   |                               |                                                                   |                                                                                              |                                                                                                    |        |               |

### AI-промпты и сравнение ассистентов

#### Промпт 1: План решения

**Задача:**
**Контекст:**
**Ограничения:**
**Ожидаемый результат:**
**Критерии успеха:**

**AI-1 ([ChatGpt]):**
Привет! Отличная задача – настроить проверку стиля кода с помощью Checkstyle в твоем современном Java-проекте на Gradle. Я научу тебя, как это сделать, шаг за шагом, и объясню логику. Код я приведу как пример, чтобы показать, как это выглядит, но главное – понять принципы.

---

### 📋 Цель: Добавить Checkstyle в Gradle Проект

Наша задача – настроить Gradle так, чтобы он мог использовать инструмент Checkstyle для проверки качества и стиля кода. Это поможет нам писать более чистый и единообразный код.

Ключевые моменты:

*   Checkstyle: Это инструмент, который проверяет твой Java-код на соответствие заданным правилам оформления (отступы, длина строк, именование и т.д.).
*   Gradle: Это система сборки, которая автоматизирует многие процессы разработки, включая запуск Checkstyle.
*   checkstyle.xml: Это файл, где прописаны все правила, которым должен следовать Checkstyle. Мы поместим его в "стандартную" директорию.
*   build.gradle (или build.gradle.kts): Это основной конфигурационный файл Gradle, где мы будем прописывать все настройки.
*   Java 25, Gradle 9.1: Мы используем самые современные версии, что обычно означает хорошую поддержку и совместимость.

и т.д (ответ слишком большой)
**AI-2 ([DeepSeek]):**
Отлично, разберём процесс добавления Checkstyle в ваш Gradle-проект шаг за шагом. Цель — сделать так, чтобы команда ./gradlew checkstyleMain успешно выполняла анализ кода.

1. Определение задачи и цели
   Вы хотите интегрировать Checkstyle — статический анализатор кода, который проверяет ваш Java-код на соответствие заданным стандартам оформления. Интеграция через Gradle 9.1 означает, что мы будем использовать современный, блочный синтаксис (в основном, Kotlin DSL, но подход аналогичен и для Groovy DSL). Файл конфигурации checkstyle.xml уже должен находиться в стандартной директории.

2. Логика процесса
   Gradle предоставляет плагины для различных задач. Checkstyle — один из таких плагинов. Нам нужно:

Применить (apply) плагин к проекту.

Настроить плагин, указав, где находится ваш файл конфигурации.

Неявным результатом станет появление новой задачи (task) checkstyleMain в жизненном цикле сборки.

Плагин автоматически добавит задачу checkstyleMain для анализа кода в src/main/java и checkstyleTest для src/test/java.
и т.д (ответ слишком большой)
**Сравнение:**
В ответах сильно понятнее для меня был DeepSeek, сильно похожего не было, кроме того что оба привели к ответу
#### Промпт 2: Проверка кода

**Код для проверки:**
public record Mentee(
String name,
String city,
String goal,
int completedLessons,
int totalLessons
) {
// Компактный конструктор с валидацией (опционально)
public Mentee {
if (completedLessons < 0 || totalLessons <= 0 || completedLessons > totalLessons) {
throw new IllegalArgumentException("Некорректные значения прогресса");
}
}
}

**Что проверить:**
1. Логические ошибки
2. Соответствие Code Conventions
3. Производительность

**AI-1 ([ChatGpt]):**
📚 Анализ record Mentee

Record представляет менторяем с базовыми данными и валидацией прогресса. Код компактный, но требует доработки по всем аспектам.

🔖 1. Логические ошибки
В компактном конструкторе отсутствуют логические операторы (||) между условиями: if (completedLessons < 0 totalLessons <= 0 completedLessons > totalLessons) интерпретируется как последовательное сравнение, а не OR-условия. Это приведёт к проверке только completedLessons < 0 и последующим операциям без разделителей, вызывая синтаксическую ошибку компиляции. Добавьте || для корректного OR: completedLessons < 0 || totalLessons <= 0 || completedLessons > totalLessons. Также валидация не проверяет name, city, goal на null/пустоту — для DTO это критично, используйте Objects.requireNonNull(name, "Name cannot be null") и аналогично для строк. Логика прогресса верна (completed ≤ total, оба ≥ 0), но totalLessons == 0 допустим только если completed == 0; уточните бизнес-правило.

🔖 2. Соответствие Code Conventions (Google Java Style)
Record следует базовым правилам: camelCase для полей, правильный отступ (2 пробела), но нарушает naming для goal (должно быть goalLearning или userGoal для ясности; используйте descriptive имена). Javadoc отсутствует — добавьте @param для каждого поля и описание record ("/** Represents a mentee's profile with progress tracking. */"). Линия if в конструкторе превышает 100 символов — разбейте или используйте fluent validation (например, ValidCheck.require().notNullOrEmpty(name).inRange(completedLessons, 0, totalLessons)). Нет suppressions для @Override (equals/hashCode генерируются автоматически). Импорты (java.util.Objects) не показаны, но требуются для requireNonNull. Общий стиль: используйте compact constructor только для validation, без бизнес-логики.

🔖 3. Производительность
Record immutable по дизайну, equals/hashCode/toString генерируются эффективно (O(1) для primitives, shallow для String). Валидация в конструкторе выполняется один раз при создании — оптимально, без runtime overhead. Проблемы: отсутствие проверки null для строк может привести к NPE в equals (решение: requireNonNull в конструкторе). Для больших объектов (если добавятся коллекции) hashCode станет дороже, но здесь поля простые — производительность отличная (меньше boilerplate, чем у class). В Java 25 используйте pattern matching в equals для оптимизации, но не нужно здесь. Масштабируемость: если Mentee в коллекциях, добавьте custom hashCode если totalLessons часто меняется (но immutable, так что нет). Тестируйте с JMH: создание 1M records займёт <1ms.
**AI-2 ([DeepSeek]):**
1. Логические ошибки
   Основная проблема:
   В условии if используется побитовый оператор ИЛИ (|) вместо логического ИЛИ (||):

java
if (completedLessons < 0 | totalLessons <= 0 | completedLessons > totalLessons)
Что не так:

| вычисляет ОБА операнда всегда (даже если первый уже true)

|| использует short-circuit evaluation (останавливается при первом true)

Здесь нужен логический оператор ||

Граничные случаи:
✅ completedLessons = 0, totalLessons = 1 — корректно

✅ completedLessons = 1, totalLessons = 1 — корректно

❌ completedLessons = 0, totalLessons = 0 — totalLessons <= 0 сработает

❌ completedLessons = 5, totalLessons = 3 — completedLessons > totalLessons сработает

Возможная скрытая проблема:
Если completedLessons может быть null (хотя int примитив), но в реальности данные могут приходить из внешних источников.

2. Соответствие Code Conventions (Java Code Style)
   Нарушения стиля:
   Имена параметров в compact constructor:

По соглашениям Java, параметры должны иметь те же имена, что и компоненты record

✅ У вас это соблюдено: name, city, goal, completedLessons, totalLessons

Пробелы вокруг операторов:

❌ Нет пробелов вокруг | в условии

❌ Не хватает пробелов после запятых в параметрах

Должно быть: completedLessons < 0 || totalLessons <= 0 || completedLessons > totalLessons

Форматирование блока:

Комментарий должен быть над конструктором, а не в той же строке

Тело конструктора должно быть на новой строке с отступом

Сообщение об ошибке:

Лучше уточнить, какие именно значения некорректны

На английском (для consistency в кодовой базе)

Пример исправленного стиля:
java
public record Mentee(
String name,
String city,
String goal,
int completedLessons,
int totalLessons
) {
// Compact constructor with validation
public Mentee {
if (completedLessons < 0 || totalLessons <= 0 || completedLessons > totalLessons) {
throw new IllegalArgumentException(
String.format(
"Invalid progress values: completedLessons=%d, totalLessons=%d",
completedLessons,
totalLessons
)
);
}
}
}
3. Производительность
   Микрооптимизации:
   Порядок условий в if:

Текущий: completedLessons < 0 | totalLessons <= 0 | completedLessons > totalLessons

Оптимальный порядок: сначала самые "дешёвые" и часто ложные условия

completedLessons < 0 — самый дешёвый (просто сравнение), но редко случается

completedLessons > totalLessons — важное бизнес-правило, должно быть на виду

Использование | вместо ||:

| вычисляет ВСЕ условия всегда → лишние вычисления

|| останавливается на первом true → потенциально быстрее

Создание исключения:

Создание IllegalArgumentException с сообщением — дорогая операция

Если это критичный по perf код, можно выбросить исключение без сообщения или использовать предсозданное

Улучшенная версия с учётом производительности:
java
public record Mentee(
String name,
String city,
String goal,
int completedLessons,
int totalLessons
) {
// Validation in compact constructor
public Mentee {
// Fast-fail validation with optimal condition order
if (completedLessons < 0) {
throw new IllegalArgumentException("completedLessons cannot be negative");
}
if (totalLessons <= 0) {
throw new IllegalArgumentException("totalLessons must be positive");
}
if (completedLessons > totalLessons) {
throw new IllegalArgumentException(
"completedLessons cannot exceed totalLessons"
);
}
}
}
Преимущества этой версии:
Быстрее: Каждое условие проверяется только при необходимости

Читаемее: Каждая проверка логически отделена

Лучшая диагностика: Точно понятно, какое правило нарушено

Легче дебажить: Stack trace указывает на конкретное условие

Выводы
Критические проблемы:
❌ Логическая ошибка: Использование | вместо || (всегда вычисляет все условия)

❌ Стиль: Плохое форматирование, отсутствие пробелов

Рекомендации:
Всегда используйте || вместо | для логических операций

Следуйте code conventions (отступы, пробелы, именование)

Для валидации нескольких условий лучше использовать раздельные if для:

Производительности (short-circuit)

Читаемости

Точности диагностики ошибок

Checkstyle найдет:
Отсутствие пробелов вокруг операторов

Плохое форматирование

Возможно, использование | вместо || (зависит от правила)

Стиль именования (если бы он нарушался)

Этот код — хороший пример того, как даже в простой конструкции можно совместить логические ошибки, стилистические проблемы и возможности для оптимизации производительности.
**Сравнение:**
Оба отметили про логический оператор, оба в целом правы, но DeepSeek более приятно для мозга объясняет, я бы выбрал ответ DeepSeek
